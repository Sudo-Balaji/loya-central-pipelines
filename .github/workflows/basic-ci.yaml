name: Central Basic CI

on:
  workflow_call:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: say hellow 
        run: echo "hellow from Central repo !! -- resuable workflow executed succesfully"
